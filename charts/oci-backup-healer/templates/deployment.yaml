apiVersion: apps/v1
kind: Deployment
metadata:
  name: oci-backup-healer
  namespace: kube-system
  labels:
    app: oci-backup-healer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oci-backup-healer
  template:
    metadata:
      labels:
        app: oci-backup-healer
    spec:
      containers:
      - name: healer
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: OCI_CONFIG_FILE
          value: "/home/nonroot/.oci/config"
        - name: OCI_PROFILE
          value: "SP-C"
        - name: PRIMARY_VM_ID
          value: "{{ .Values.primaryVMID }}"
        - name: BACKUP_VM_ID
          value: "{{ .Values.backupVMID }}"
        - name: VOLUME_ID
          value: "{{ .Values.volumeID }}"
        - name: BACKUP_VM_IP
          value: "{{ .Values.backupVMIP }}"
        - name: SSH_KEY_PATH
          value: "/home/nonroot/.ssh/ssh_private_key"
        - name: RECONCILE_INTERVAL_SECS
          value: "{{ .Values.reconcileIntervalSecs }}"
        - name: UNHEALTHY_TIMEOUT_SECS
          value: "{{ .Values.unhealthyTimeoutSecs }}"
        - name: DRY_RUN
          value: "{{ .Values.dryRun }}"
        - name: FAKE_PROVIDER
          value: "{{ .Values.fakeProvider }}"
        - name: NTFY_TOPIC_URL
          value: "{{ .Values.ntfyTopicURL }}"
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 32Mi
        volumeMounts:
        - name: oci-config
          mountPath: /home/nonroot/.oci
          readOnly: true
        - name: ssh-key
          mountPath: /home/nonroot/.ssh
          readOnly: true
      volumes:
      - name: oci-config
        secret:
          secretName: {{ .Values.ociConfigSecret }}
          defaultMode: 0444
      - name: ssh-key
        secret:
          secretName: {{ .Values.sshKeySecret }}
          defaultMode: 0444
      nodeSelector:
        kubernetes.io/hostname: oci-ash-a-micro-1
      tolerations:
      - key: "node-role.kubernetes.io/master"
        operator: "Exists"
        effect: "NoSchedule"
      - key: "node-role.kubernetes.io/control-plane"
        operator: "Exists"
        effect: "NoSchedule"
